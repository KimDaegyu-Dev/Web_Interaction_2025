# Week 9 - Oblique Projection with Three.js

리팩토링된 Three.js 프로젝트로, Oblique 투영을 구현한 3D 갤러리 룸입니다.

## 📁 프로젝트 구조

```
week9/
├── main.js                      # 메인 진입점
├── index.html                   # HTML 엔트리
├── ObliqueControls.js           # 레거시 컨트롤 (호환성)
├── main_old.js                  # 백업된 원본 파일
└── src/
    ├── animation/
    │   └── animationLoop.js     # 애니메이션 루프 관리
    ├── cameras/
    │   └── setupCameras.js      # 카메라 설정
    ├── config/
    │   ├── constants.js         # 상수 정의
    │   └── params.js            # 투영 파라미터 및 프리셋
    ├── controls/
    │   └── ObliqueControls.js   # Oblique 투영 컨트롤
    ├── gui/
    │   └── setupGUI.js          # lil-gui 설정
    ├── lights/
    │   └── setupLights.js       # 조명 설정
    ├── objects/
    │   ├── decorations.js       # 장식 요소 (액자, 기둥, 바닥 패턴 등)
    │   ├── displayObjects.js    # 전시 오브젝트 (큐브, 받침대)
    │   └── room.js              # 방 구조 (바닥, 천장, 벽)
    ├── scenes/
    │   └── mainScene.js         # 씬 설정
    └── utils/
        ├── interaction.js       # 호버 인터랙션
        ├── projection.js        # Oblique 투영 행렬 계산
        └── renderer.js          # 렌더러 설정
```

## 🎯 리팩토링 주요 개선 사항

### 1. **관심사의 분리 (Separation of Concerns)**
- 각 기능별로 독립적인 모듈로 분리
- 씬, 오브젝트, 라이트, 카메라 등을 별도 파일로 관리

### 2. **단일 책임 원칙 (Single Responsibility Principle)**
- 각 함수와 클래스가 하나의 명확한 책임만 가짐
- `AnimationLoop` 클래스: 애니메이션 루프만 담당
- `HoverInteraction` 클래스: 마우스 인터랙션만 담당

### 3. **재사용성 향상**
- 모듈화된 함수들로 다른 프로젝트에서도 쉽게 재사용 가능
- 각 오브젝트 생성 함수를 독립적으로 사용 가능

### 4. **유지보수성 개선**
- 기능별로 파일이 분리되어 특정 기능 수정 시 해당 파일만 수정
- 명확한 폴더 구조로 원하는 코드를 빠르게 찾을 수 있음

### 5. **코드 가독성 향상**
- main.js가 간결해져서 전체 앱의 흐름을 한눈에 파악 가능
- 각 모듈이 명확한 이름을 가져 기능을 쉽게 이해

## 🚀 실행 방법

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

## 🎮 기능

- **Oblique 투영**: 사용자 정의 가능한 축 각도와 스케일
- **프리셋**: Isometric, Dimetric, Front Oblique, Cabinet
- **인터랙션**: 마우스 호버 시 오브젝트 확대
- **컨트롤**: 마우스 드래그로 패닝 가능
- **애니메이션**: 큐브 회전, 조명 효과

## 📚 모듈 설명

### Config
- `constants.js`: 방 크기, 높이 등 상수 정의
- `params.js`: 투영 파라미터 및 프리셋 설정

### Scenes
- `mainScene.js`: 메인 씬, 축 씬 생성

### Objects
- `room.js`: 바닥, 천장, 벽 생성
- `displayObjects.js`: 큐브와 받침대 생성
- `decorations.js`: 액자, 조명, 기둥, 바닥 패턴 등

### Lights
- `setupLights.js`: 앰비언트, 포인트, 방향성 조명 설정

### Cameras
- `setupCameras.js`: 메인 카메라 및 축 카메라 설정

### Utils
- `projection.js`: Oblique 투영 행렬 계산
- `interaction.js`: 마우스 호버 감지 및 처리
- `renderer.js`: WebGL 렌더러 설정

### Animation
- `animationLoop.js`: 메인 애니메이션 루프 관리 클래스

### GUI
- `setupGUI.js`: lil-gui 인터페이스 설정

### Controls
- `ObliqueControls.js`: 마우스 드래그로 패닝 기능

## 🔧 기술 스택

- **Three.js**: 3D 렌더링
- **lil-gui**: GUI 컨트롤
- **Vite**: 빌드 도구

## 📝 라이센스

MIT

